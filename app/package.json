{
  "name": "github-actions-training-app",
  "version": "1.0.0",
  "description": "Sample Express app for GitHub Actions CI/CD training labs",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "node --test",
    "lint": "node --check src/app.js && node --check test/app.test.js && node --check scripts/build.js",
    "build": "node scripts/build.js"
  },
  "keywords": [
    "github-actions",
    "ci",
    "devops",
    "training"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0"
  }
}
